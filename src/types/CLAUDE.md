# Types

## Rules
- PascalCase filenames and type/interface names (e.g., `UserType.ts`, `ConfigType.ts`)
- Prefer `type` over `interface` unless declaration merging is needed
- Organized by domain: root-level for cross-cutting types, `data/` for entity types
- Use named exports
